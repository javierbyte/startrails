# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/dist
/.next
/out
.vercel
/node_modules
/.pnpm-store
/.pnp
.pnp.js

# testing
/coverage

# generated by next dev; kept local, not tracked
/AGENTS.md
/CLAUDE.md

# production
/build

# Working material: source frames for testing the tool. public/ is copied into
# the static export, but a clean CI checkout never sees these, so nothing
# reaches gh-pages either.
/public/timelapse

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Personal, machine-local Claude permissions
.claude/settings.local.json
